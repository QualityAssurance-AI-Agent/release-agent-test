{
  "name": "pay-console",
  "version": "1.0.0",
  "private": true,
  "description": "The payments search UI.",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "test:ui": "playwright test",
    "lint": "eslint src --max-warnings 0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "eslint": "^9.10.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
